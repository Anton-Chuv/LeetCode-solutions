#include <iostream>

using namespace std;

class Solution {
public:
    vector<int> productQueries(int n, vector<vector<int>>& queries) {
        vector<int> out;

        return out;
    }
};

int main() {
    Solution solution;
    int n = 2;
    vector<vector<int>> queries = {};
    for (int x: solution.productQueries(n, queries))
        cout << x << ' ';
    return 0;
}
